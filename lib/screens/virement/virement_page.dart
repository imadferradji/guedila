import 'package:flutter/material.dart';

class VirementPage extends StatelessWidget {
  const VirementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Écran Virement")),
    );
  }
}
